# API server port for tests
PORT=3000

# Test database connection string
# Use a separate test database to avoid conflicts
DATABASE_URL=postgresql://username:password@localhost:5433/database_name_test

# Redis connection string for tests
REDIS_URL=redis://localhost:6379
