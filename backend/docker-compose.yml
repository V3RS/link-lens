version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      # Development only - use proper secrets in production
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: linklens
    ports: ["5433:5432"]

  redis:
    image: redis:7
    ports: ["6379:6379"]
